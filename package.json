{
  "name": "helix-page-lens",
  "version": "1.0.0",
  "private": true,
  "description": "Local-first Chrome page analysis extension",
  "scripts": {
    "check": "npm run lint && npm test && npm run build",
    "lint": "node scripts/check.mjs",
    "test": "node --test",
    "build": "node scripts/build.mjs"
  },
  "engines": { "node": ">=20" }
}
